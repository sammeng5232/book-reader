# fixtures/ (generated by make_fixtures.py, deterministic) and out/ (screenshots from the
# GUI suites) are committed so the repository carries a known-good test corpus and
# reference screenshots.  Rebuild fixtures with:  python314 tests\make_fixtures.py --clean --verify

__pycache__/
*.pyc
